apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.name }}
  labels:
    app: {{ .Values.name }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.name }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0   # this defines zero downtime rollout
      maxSurge: 1
  template:
    metadata:
      labels:
        app: {{ .Values.name }}
    spec:
      containers:
        - name: {{ .Values.name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
          # Credentials/config come from Vault. The Vault Secrets Operator syncs
          # each declared Vault path into a native Secret (see vault-secret.yaml),
          # which we mount here via envFrom. Nothing sensitive is ever in Git.
          envFrom:
            {{- if .Values.vault.enabled }}
            {{- range .Values.vault.sources }}
            - secretRef:
                name: {{ $.Values.name }}-{{ .name }}
            {{- end }}
            {{- else }}
            - secretRef:
                name: {{ .Values.envFromSecret }}
            {{- end }}
          # Liveness restarts hung pods; readiness (DB-aware) gates traffic so a
          # pod receives no requests until its database connection is healthy.
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness }}
              port: {{ .Values.containerPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness }}
              port: {{ .Values.containerPort }}
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: {{ .Values.resources.requests.cpu }}
              memory: {{ .Values.resources.requests.memory }}
            limits:
              cpu: {{ .Values.resources.limits.cpu }}
              memory: {{ .Values.resources.limits.memory }}
